# payment-gateway
 A backend simulation of a payment gateway built in C++. It supports UPI, Card, and Wallet payments using the Strategy Design Pattern. The system manages transactions with unique IDs, retry logic for failed payments, and structured logging, demonstrating modular design and core backend engineering concepts.
